{
  "name": "gcode",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "c8": "^7.12.0",
    "vite": "^3.0.7",
    "vitest": "^0.21.1"
  },
  "dependencies": {
    "@floating-ui/dom": "^1.0.1"
  }
}
